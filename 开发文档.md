# 仓库管理系统开发文档（wcs）

本文档以当前代码实现为准，覆盖本地开发、Docker 部署、数据迁移、关键功能（出库单号、在途、打印/导出、模板上传）与已知待完善点。

## 1. 项目概述

### 1.1 技术栈
- 后端：FastAPI + SQLAlchemy + Alembic（Python 3.10）
- 前端：Vue 3 + Vite + Element Plus
- 数据库：PostgreSQL
- 导入导出/打印：pandas + openpyxl（Excel 导入导出、打印申请单）

### 1.2 功能模块（现状）
| 模块 | 说明 |
|---|---|
| 基础数据 | 物料管理、库位管理（仓库/库区/库位编码）、价格版本（批次/合同号） |
| 入库管理 | 入库登记、在途入库（支持部分入库） |
| 出库管理 | 出库登记（统一出库单号按日期递增）、出库记录导出、申请单下载（单张/批量） |
| 在途库存 | 箱号+物料维度的在途记录、支持箱号/物料编码搜索、显示待入库/已入库数量 |
| 实时库存 | 库存按物料/库位/批次查询，展示库存与金额信息（按价格版本） |
| 操作流水 | 出入库/移库/盘点等库存流水查询，支持按日期段导出 Excel |
| 系统管理 | 用户管理、数据清理、模板管理（上传/下载申请单模板） |

## 2. 代码结构
```
wcs/
  backend/         FastAPI 服务（app/）
  frontend/        Vue3 前端（src/）
  docker-compose.yml
  开发文档.md
```

## 3. 本地开发

### 3.1 后端启动
在 `wcs/backend` 目录：
- 安装依赖：`pip install -r requirements.txt`
- 初始化数据库：`alembic upgrade head`
- 启动：`uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`

默认 API 前缀：`/api/v1`，可访问 OpenAPI：`/api/v1/openapi.json`

### 3.2 前端启动
在 `wcs/frontend` 目录：
- 安装依赖：`npm i`
- 启动：`npm run dev`

说明：部分下载/上传接口在前端使用了 `http://127.0.0.1:8000` 的固定地址（用于 Excel 下载、模板上传等），如需适配不同环境建议统一改为环境变量。

## 4. Docker 部署

### 4.1 一键启动
在 `wcs/` 目录执行：
```bash
docker-compose up -d
```

默认端口（以当前 docker-compose.yml 为准）：
- Postgres：`5432`
- 后端：宿主机 `5001` → 容器 `5000`
- 前端：宿主机 `8081` → 容器 `80`

### 4.2 配置项
后端关键环境变量（docker-compose.yml）：
- `DATABASE_URL`
- `SECRET_KEY`
- `ALGORITHM`
- `ACCESS_TOKEN_EXPIRE_MINUTES`

## 5. 数据库与迁移
- 迁移目录：`wcs/backend/alembic/versions`
- 升级：`alembic upgrade head`
- 生成迁移：`alembic revision --autogenerate -m "xxx"`

近期字段演进要点：
- `outbound_order`：新增统一出库单号 `group_no`（按日期递增生成），明细行号 `order_no` 仍保持唯一
- `transit_inventory`：新增 `total_quantity`/`received_quantity`，`quantity` 表示待入库数量
- `location`：新增 `warehouse_code`/`zone_code`/`location_code` 三段编码（`code` 作为完整编码）

## 6. 关键业务规则

### 6.1 库位编码（仓库/库区/库位）
`location` 表包含：
- `warehouse_code`：仓库编码
- `zone_code`：库区编码
- `location_code`：库位编码
- `code`：完整编码（默认拼接：`warehouse-zone-location`）

接口 `POST /api/v1/locations/`：
- 既支持直接传 `code`
- 也支持传三段编码（后端自动拼接 `code`）

### 6.2 出库单号（统一单号）
创建出库单时，如不填写单号，后端生成统一单号：
- 格式：`OUT-YYYYMMDD-001`
- 同一天按序递增

出库单明细仍有行号（用于唯一性），但列表展示使用统一单号。

### 6.3 在途入库（支持部分入库）
- 在途记录使用 `quantity` 表示“待入库数量”，`received_quantity` 表示“已入库数量”
- 部分入库后会扣减待入库；当待入库为 0，会删除该条在途记录（整箱全部入库后箱号不再保留）

## 7. 打印与导出（Excel）

### 7.1 申请单下载（单张）
接口：
- `GET /api/v1/excel/print/outbound/{group_no}?rows=10`

说明：
- `rows` 为最少显示行数，明细不足会补空行，明细超过会自动扩展
- 如系统上传了模板，会优先用模板填充；否则使用内置格式生成

### 7.2 申请单批量下载（按客户+日期段）
接口：
- `GET /api/v1/excel/print/outbound-batch?customer=xxx&start_date=YYYY-MM-DD&end_date=YYYY-MM-DD&rows=10`

返回一个 Excel，按出库单号分 Sheet（每张单一张 Sheet）。

### 7.3 操作流水导出（按日期段）
接口：
- `GET /api/v1/excel/export/transactions?start_date=YYYY-MM-DD&end_date=YYYY-MM-DD`

### 7.4 模板管理（系统管理）
系统管理提供模板上传/下载（申请单 Excel 模板）：
- 下载当前模板：`GET /api/v1/system/templates/outbound-request`
- 上传模板（xlsx）：`POST /api/v1/system/templates/outbound-request`

模板文件保存位置：
- `wcs/backend/app/templates/outbound_request_template.xlsx`

模板填充约定（默认模板坐标）：
- `H2`：单据编号
- `A3`：申领部门/单位（客户）
- `F3`：领用日期
- 明细起始行：第 5 行；列顺序：序号、物料编码、物资名称、型号/备件号、品牌、单位、申请数量、使用设备、备注

## 8. 已知待完善点（建议优先级）

### P0（必须尽快）
- 后端路由鉴权未统一接入：建议实现 `get_current_user` + RBAC，对系统管理/清库/模板上传等高危操作强制管理员权限
- CORS `*` 与 credentials 组合风险：建议收敛到白名单
- SECRET_KEY 默认值/明文写死风险：建议生产环境强制外部注入并 fail-fast

### P1（尽快）
- 请求参数约束不完整：建议对数量/ID/字符串长度等增加 Pydantic 校验
- operator_id 应由 token 派生：避免前端伪造操作人
- Excel 导入/导出缺少大小/范围限制：建议限制上传体积与最大行数，大导出任务化
- 路由可达性检查：确保所有导入/导出/管理接口都注册到 OpenAPI 并有 smoke test

### P2（中期）
- Docker/compose 生产化：去掉 `--reload`、DB 不对外暴露、nginx 安全头/上传限制/限流
- 打印配置统一：前端 localStorage 配置与后端模板上传两套并存，建议确定主方案并统一

## 9. 鉴权与权限（RBAC）

### 9.1 鉴权方式
- 前端登录调用：`POST /api/v1/auth/login/access-token`（OAuth2 Password Flow）
- 后端签发 JWT，前端将 token 写入 `localStorage` 并在请求头携带：`Authorization: Bearer <token>`
- 后端对除 `/auth/*` 的登录接口外的业务接口默认要求登录态

### 9.2 角色与权限
- `admin`：可访问系统管理（用户管理、清空数据、模板上传/下载）
- `warehouse_manager`：仅可访问业务模块（入库、出库、在途、库存、操作流水等），不可访问 `/system/*`

### 9.3 初始化管理员
后端提供脚本初始化默认管理员（不会随服务启动自动执行）：
- 脚本：`wcs/backend/init_admin.py`
- 默认账号：`admin`
- 默认密码：`123456`（可通过环境变量 `ADMIN_PASSWORD` 覆盖）

示例：
```bash
cd wcs/backend
export ADMIN_PASSWORD='你的强密码'
python init_admin.py
```
